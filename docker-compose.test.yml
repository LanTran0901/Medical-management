# Chạy pytest trong container với PostgreSQL (không cần MongoDB).
#
#   docker compose -f docker-compose.test.yml run --rm --build test
#   docker compose -f docker-compose.test.yml down -v --remove-orphans
#
# Chỉ unit test (nhanh, không cần DB):
#   docker compose -f docker-compose.test.yml run --rm --build test \
#     sh -c "pytest tests/test_family_permission.py -v --no-cov"

services:
  postgres:
    image: postgres:16-alpine
    container_name: medical-postgres-test
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: medical
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d medical"]
      interval: 3s
      timeout: 5s
      retries: 15
    networks:
      - medical-test-net

  test:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        INSTALL_TEST_DEPS: "1"
    container_name: medical-api-test
    restart: "no"
    env_file:
      - docker/test.env
    environment:
      HOMEDMEDAI_INTEGRATION: "1"
      SKIP_MONGO_LIFESPAN: "1"
    depends_on:
      postgres:
        condition: service_healthy
    working_dir: /app
    volumes:
      - .:/app
    entrypoint: []
    command: >
      sh -c "cd /app/app && alembic --config alembic.ini upgrade head &&
             cd /app && pytest tests/ -v --tb=short --no-cov"
    networks:
      - medical-test-net

networks:
  medical-test-net:
    driver: bridge
